# Core scientific computing - use ~= for compatible releases
numpy~=1.24.0      # Allows 1.24.x, not 2.0.0
torch~=2.0.0       # Allows 2.0.x, not 2.1.0
scipy~=1.10.0      # Allows 1.10.x, not 1.11.0

# Visualization
matplotlib~=3.7.0  # Allows 3.7.x
plotly~=5.15.0     # Allows 5.15.x

# Data handling
pandas~=2.0.0      # Allows 2.0.x
openpyxl~=3.1.0    # Allows 3.1.x

# Terminal UI
rich~=13.4.0       # Allows 13.4.x

# Signal Processing (already covered by scipy above)

# Data Sources (optional)
yfinance~=0.2.0    # For market data examples
requests~=2.31.0   # For web APIs

# Optional: QRNG Integration
# websocket-client~=1.6.0  # For live QRNG streaming

# ============================================
# GPU Acceleration (cuQuantum) - RTX 5090
# ============================================
# For CUDA 12.x (RTX 5090 / Blackwell architecture)
cupy-cuda12x>=13.0.0           # GPU array operations
cuquantum-python-cu12>=24.11   # NVIDIA cuQuantum SDK
cutensornet-cu12>=2.0.0        # Tensor network library

# Optional quantum circuit libraries (for simulation comparison)
cirq>=1.3.0                    # Google's quantum circuit library
qiskit>=1.0.0                  # IBM's quantum SDK

# BiEntropy calculation
bitstring>=4.1.0               # Bit manipulation for entropy

# Development tools
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
black>=22.0.0
mypy~=1.4.0        # Static type checker

# Note: For exact reproducible builds, use requirements-pinned.txt instead
# Note: cuQuantum requires NVIDIA GPU with CUDA compute capability 7.0+
